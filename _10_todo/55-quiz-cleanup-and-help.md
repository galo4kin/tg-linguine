# 55 — Финальная чистка и обновление помощи

## Цель
Закрыть фичу: удалить хвосты старого режима, обновить тексты помощи и команды бота, причесать i18n.

## Что сделать
- Прогрепать проект и удалить мёртвый код карточечного режима (если что-то не выпилили в шаге 50).
- Обновить `bot.SetMyCommands` (или эквивалент): `/study` — «Тренировка слов (квиз)»; `/me` — «Прогресс» (если добавили в 54).
- Обновить README и любые help-тексты (`/help`, стартовое сообщение).
- Прогнать все три локали (`en`, `es`, `ru`) на полноту: ни одного отсутствующего ключа `quiz.*` / `me.*`.
- Проверить, что в логах нет шума от устаревших callback'ов.

## Definition of Done
- Финальная end-to-end проверка из плана (см. `valiant-floating-matsumoto.md`, секция «Верификация»).
- `make build` / `make test`.
- Один коммит `step 55: quiz-cleanup-and-help`.

## Ревью
Шаг 55 кратен 5 — после закрытия задачи провести code review всей фичи и при необходимости создать рефактор-задачи.
