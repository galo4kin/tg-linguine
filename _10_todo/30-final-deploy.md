# Шаг 30. Финальный деплой и приёмка (2–3h)

## Цель
v1.0 на mac mini. Все 17 пунктов критериев приёмки из ТЗ выполнены.

## Действия
1. Перевыкатка финального бинарника на mac mini (см. процесс из
   шага 14). Тег версии в git: `v1.0.0`.
2. Бэкап `bot.db` (например, `cp bot.db bot.db.v1.0.bak` в
   защищённую папку или scp на резерв).
3. Прохождение чек-листа приёмки ТЗ — 17 пунктов. Для каждого —
   галочка либо комментарий, что не выполнено и почему.
4. Краткий пост-релиз отчёт (1 абзац) в README или в
   `done/30-final-deploy.md`: что сделано, что отложено.

## DoD
- Все 17 пунктов критериев приёмки выполнены либо явно
  задокументированы как отложенные.
- Бот версии v1.0 работает на mac mini, пережил тестовый reboot.
- `bot.db` забэкаплен.
- Тег `v1.0.0` существует в git.
- `make build` + `make test` зелёные.

## Зависимости
Шаг 29.
