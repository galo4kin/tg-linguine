# Step 42.5 — refactor-analyzed-article-naming

## Откуда

Code review шага 40, finding §1: [_30_done/40-groq-respect-retry-after-review.md](_30_done/40-groq-respect-retry-after-review.md).

## Проблема

`AnalyzedArticle.Article *Article` + `AnalyzeResult.Article *AnalyzedArticle` дают цепочки `result.Article.Article.ID` (видно в `internal/articles/usecase_test.go:120, 123, 375, 376`). Поле `Article` перегружено на двух уровнях.

## Что делаем

1. В `internal/articles/usecase.go` переименовать поле `AnalyzedArticle.Article` → `AnalyzedArticle.Stored`.
2. Поправить все обращения:
   - `internal/articles/usecase.go` (cache hit, AnalyzeExtracted, runAnalysis return).
   - `internal/articles/usecase_test.go` (`first.Article.Article.* → first.Article.Stored.*`, и т.д.).
   - `internal/articles/long_test.go` (`analyzed.Article.* → analyzed.Stored.*`).
   - `internal/telegram/handlers/url.go` (`renderResult` → `analyzed.Stored`).
   - `internal/telegram/handlers/long_article.go` (`analyzed.Stored`).
   - `internal/telegram/handlers/long_integration_test.go` если применимо.

## DoD

- `make build && make test` зелёные.
- Один коммит `step 42.5: refactor-analyzed-article-naming`.
- `pkill -f bin/tg-linguine` (production-код менялся → respawn).
